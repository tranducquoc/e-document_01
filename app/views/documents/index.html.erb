<div class="top2">
  <div class="container">
    <h2 class="text-center"><%= t ".new_documents" %></h2>
    <div class="row newest-product">
      <%= render @document_support.newest_documents %>
    </div>
    <% if user_signed_in? %>
      <h2 class="best-sl text-center"><%= t ".own_documents" %></h2>
      <div class="row best-product">
        <%= render @document_support.own_documents current_user %>
      </div>
    <% end %>
  </div>
</div>
