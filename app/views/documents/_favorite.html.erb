<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :document_id, @document.id %></div>
  <%= button_tag "", class: "btn btn-default button-2", id: "btn_favorite" do %>
    <i class="fa fa-gratipay"></i>
    <span><%= t ".like" %></span>
  <% end %>
<% end %>
